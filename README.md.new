exa
===============================

The exa framework for data processing analytics,a and visualization.

Installation
------------

To install use pip:

    $ pip install exa
    $ jupyter nbextension enable --py --sys-prefix exa


For a development installation (requires npm),

    $ git clone https://github.com/exa/exa.git
    $ cd exa
    $ pip install -e .
    $ jupyter nbextension install --py --symlink --sys-prefix exa
    $ jupyter nbextension enable --py --sys-prefix exa
